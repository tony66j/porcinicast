# Requirements OTTIMIZZATI per Render - Risolve conflitti dipendenze
# Versioni testate e compatibili tra loro

# === CORE WEB FRAMEWORK ===
fastapi==0.104.1
uvicorn[standard]==0.24.0

# === HTTP CLIENT ===
httpx==0.25.2

# === SCIENTIFIC COMPUTING ===
# Installate via Dockerfile con --only-binary
# numpy e scipy gestiti dal Dockerfile

# === GEOSPATIAL E CLIMATE DATA ===
cdsapi==0.6.7
netCDF4==1.6.2

# === UTILITIES - VERSIONI COMPATIBILI ===
python-multipart==0.0.5
python-json-logger==2.0.4

# === GEOSPATIAL ===
geohash2==1.1

# === PERFORMANCE ===
uvloop==0.17.0
httptools==0.5.0# Requirements per modello SUPER AVANZATO v2.5.0
# Usa pre-compiled wheels via Dockerfile per evitare errori build

# === CORE WEB FRAMEWORK ===
fastapi==0.111.0
uvicorn[standard]==0.30.1

# === HTTP CLIENT ===
httpx==0.27.0

# === SCIENTIFIC COMPUTING AVANZATO ===
# Installate via Dockerfile con --only-binary per evitare compilation
# numpy==1.24.4  # Installato via Dockerfile
# scipy==1.10.1  # Installato via Dockerfile

# === GEOSPATIAL E CLIMATE DATA ===
cdsapi==0.7.4
netCDF4==1.6.5

# === UTILITIES ===
python-multipart==0.0.6
python-json-logger==2.0.7

# === GEOSPATIAL ===
geohash2==1.1

# === PERFORMANCE ===
uvloop==0.19.0
httptools==0.6.1

# === OPTIONAL DEV DEPENDENCIES ===
# pytest==7.4.4
# pytest-asyncio==0.23.2
